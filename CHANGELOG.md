# Changelog

## [1.2.3] - 2026-09-12

### Changed

- 元数据维护：`logo` 统一使用仓库绝对链接（raw.githubusercontent.com），与其它插件格式一致；版本号同步刷新，便于云端识别更新。

## [1.2.2] - 2026-09-07

### Changed

- `bot_name_map`、`bot_name_by_self_id` 由 dict 键值对（键/值/类型下拉）改为更直观的列表式编辑：
  点开配置项后，直接填「QQ号,昵称」（一行一条，半角逗号）再点「添加」即可，不用再区分键和值、也不用选值类型。
- 旧版本 dict 与 `k:v,k:v` 字符串格式仍兼容读取，存量配置不受影响。

## [1.2.1] - 2026-09-07

### Changed

- `bot_name_map`、`bot_name_by_self_id` 配置项由字符串改为可视化 dict 编辑：在面板点「添加」填键值即可
  （键=群号/Bot的QQ号，值=昵称），不再需要手写 `k:v,k:v` 格式；旧字符串格式仍兼容读取。
- `bot_name` 文案改为「默认昵称/唤醒词（兜底）」，明确其兜底语义。

## [1.2.0] - 2026-09-07

### Added

- 新增 `bot_name_by_self_id` 配置，可按 Bot 账号(self_id)设置不同昵称（如 `10001:宁宁,10002:小助手`）。
  多账号挂载时，私聊也能正确区分唤醒词。
- 昵称解析优先级：按群 `bot_name_map` > 按Bot `bot_name_by_self_id` > 全局 `bot_name` > 默认 `宁宁`。

### Changed

- 抽取通用 `_parse_kv_map`，统一按群/按Bot 两种映射的解析逻辑。

## [1.1.0] - 2026-08-30

### Added

- 多昵称支持：新增 `bot_name_map` 配置，可按群设置不同昵称（如 `748791823:老大`）。
- 新增 `exclude_groups` 配置，可跳过指定群的提示词补全。
- 新增 `enable_private` 配置，可关闭私聊场景的补全。
- README 增加修复前后对比表、Mermaid 原理图、徽章。

### Changed

- 重构昵称解析逻辑，按群回退到全局默认昵称。

## [1.0.0] - 2026-08-28

### Added

- 首次发布。
- 通过 `on_llm_request` 钩子补全被唤醒词剥离的提示词。
- `@机器人` 自动替换为昵称（默认 `宁宁`）。
- 支持 `bot_name`、`keep_at_marker` 配置项。
- 多轮对话与工具调用场景保护。
